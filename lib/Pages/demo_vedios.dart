
import 'package:flutter/material.dart';

class DemoVedios extends StatelessWidget {
  const DemoVedios({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Demo Vedio"),
    );
  }
}
